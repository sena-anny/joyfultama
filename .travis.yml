language: node_js

node_js:
  - "8.0"

script:
  - echo "Deploy for deveop"

install:
  - npm install -g firebase-tools

after_success:
  - firebase use joyful-tama-dev
  - firebase deploy --token $FIREBASE_TOKEN

